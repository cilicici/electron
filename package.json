{
  "name": "electron-auto-update-example",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/cilicici/electron.git"
  },
  "build": {
    "publish": [{
      "provider": "github",
      "owner": "cilicici",
      "repo": "electron"
    }]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always",
    "release": "npm test && webpack --config=build/webpack.app.config.js --env=production && electron-builder --mac --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.1",
    "electron-builder": "^22.8.0"
  },
  "dependencies": {
    "electron-updater": "^4.3.4"
  }
}
